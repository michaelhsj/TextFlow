experiment_name: "db_resnet50_text_ocr"
model_uri: "db_resnet50_det_model"

data:
  train_num_samples: 32
  val_num_samples: 16
  train_batch_size: 1
  val_batch_size: 1

path:
  model_dir: "model"
  dataset_dir: "dataset/TextOCR"
  image_dir: "train_val_images"
  train_json: "TextOCR_0.1_train.json"
  val_json: "TextOCR_0.1_val.json"

model:
  pretrain: true
  lr: 0.001
  num_epochs: 2
  train_interval: 1 # batches per training loss log
  val_interval: 2 # batches per validation loss check